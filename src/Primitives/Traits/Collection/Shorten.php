<?php

declare(strict_types=1);

namespace Atournayre\Primitives\Traits\Collection;

/**
 * Trait Shorten.
 */
trait Shorten
{
    use After;
    use Before;
    use Clear;
    use Diff;
    use DiffAssoc;
    use DiffKeys;
    use Except;
    use Filter;
    use Grep;
    use Intersect;
    use IntersectAssoc;
    use IntersectKeys;
    use Nth;
    use Only;
    use Reject;
    use Remove;
    use Skip;
    use Slice;
    use Take;
    use Where;
}
