<?php

declare(strict_types=1);

namespace Atournayre\Primitives\Traits\Collection;

/**
 * Trait Test.
 */
trait Test
{
    use Compare;
    use Contains;
    use Each;
    use Empty_;
    use Equals;
    use Every;
    use Has;
    use If_;
    use IfAny;
    use IfEmpty;
    use In;
    use Includes;
    use Is;
    use IsEmpty;
    use IsNumeric;
    use IsObject;
    use IsScalar;
    use Implements_;
    use None;
    use Some;
    use StrContains;
    use StrContainsAll;
    use StrEnds;
    use StrEndsAll;
    use StrStarts;
    use StrStartsAll;
    use StrBefore;
}
